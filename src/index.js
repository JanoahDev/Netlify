import { devineLog, sum } from './js/utility/helpers.js';
{
    const init = () => {
        console.log(`yeet`);
        devineLog(`Ik gebruik de devineLog functie`);
        sum(10, 2);
    }

    init();
}